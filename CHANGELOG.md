# لیست تغییرات (Changelog)

تمام تغییرات مهم و قابل توجه این پروژه در این فایل مستند خواهند شد.
## [2.0.1] - 2026-06-06

### اضافه شده
- **اسکن پس‌زمینه هوشمند برای پوشه‌ها:** حالا وقتی یک پوشه (مخصوصا پوشه‌های سنگین چند ده گیگابایتی) رو از طریق "Send To" یا کشیدن و رها کردن وارد برنامه می‌کنید، برنامه به جای هنگ کردن، در کسری از ثانیه باز میشه و عملیات جستجوی عکس‌ها رو در پس‌زمینه (با ارائه آمار زنده در پایین صفحه) انجام میده تا هیچ وقت قفل نکنه.

### تغییر یافته
- **یکپارچه‌سازی کامل حالت ساده و پیشرفته:** حالت اجرای کاملا مخفیِ برنامه (بدون واسط گرافیکی) حذف و با حالت گرافیکی ادغام شد. حالا اگر فایلی رو بدون نگه داشتن کلید Shift روی برنامه رها کنید (یا از کلیک راست استفاده کنید)، محیط گرافیکی بالا میاد اما دکمه فشرده‌سازی به صورت **خودکار** فشرده شده و عملیات با تنظیمات پیش‌فرض شروع میشه و در نهایت هم گزارش تصویری نتایج به شما نمایش داده میشه.
- **تغییر نام در منوی کلیک راست:** نام برنامه در منوی "Open With" ویندوز از `compressor.exe` به نام زیباترِ `Ultimate Image Compressor` تغییر پیدا کرد.

## [2.0.0] - 2026-06-05

### اضافه شده
- **رابط کاربری مدرن:** بازطراحی کامل رابط کاربری با استفاده از `customtkinter` با پشتیبانی کامل از پوسته‌های تاریک (Dark) و روشن (Light).
- **پشتیبانی از زبان فارسی (RTL):** اضافه شدن پشتیبانی کامل از زبان فارسی با چیدمان راست‌به‌چپ (RTL)، متن‌های بومی‌سازی شده و فونت زیبای `Vazirmatn`.
- **پنل مدیریت فایل‌ها:** یک لیست تصویری جدید که به شما اجازه می‌دهد فایل‌های متعددی را اضافه، حذف و به راحتی مدیریت کنید.
- **پیش‌نمایش زنده و زوم:** بوم پیش‌نمایش تعاملی عکس با قابلیت جابجایی (Drag)، زوم با اسکرول ماوس و نمایش اطلاعات تصویر اصلی (ابعاد، فرمت، حجم).
- **حالت فشرده‌سازی بر اساس حجم هدف:** اضافه شدن الگوریتم هوشمند که تلاش می‌کند تصویر را دقیقاً به حجم تعیین شده (بر حسب کیلوبایت) فشرده کند.
- **جلوگیری از هنگ کردن (Timeout):** اعمال محدودیت ۶۰ ثانیه‌ای روی پردازشگرها تا در مواجهه با عکس‌های بسیار حجیم یا خراب، برنامه قفل نکند.
- **سیستم بازیابی اطلاعات (Crash Recovery):** برنامه اکنون صف فایل‌های پردازش‌نشده را به خاطر می‌سپارد. اگر برنامه بسته شود یا کرش کند، در اجرای بعدی از شما می‌پرسد که آیا مایل به ادامه کار روی فایل‌های باقی‌مانده هستید یا خیر.
- **مدیریت هوشمند خطاها:** گزارش‌های نهایی اکنون دلیل دقیق خطاها را نمایش می‌دهند. همچنین دکمه جدید "تلاش مجدد برای ناموفق‌ها" اضافه شده که با زدن آن، فایل‌های موفق از لیست پاک شده و فقط فایل‌های دارای ارور برای پردازش مجدد باقی می‌مانند.

### تغییر یافته
- بهبود سیستم کشیدن و رها کردن (Drag & Drop) برای انتقال روان فایل‌ها به لیست برنامه در حالت پیشرفته.
- تغییر ساختار ابزارهای داخلی. حالا از مسیرهای مطلق برای استخراج فایل‌های `PyInstaller` استفاده می‌شود تا بیشترین سازگاری را در تمام سیستم‌های ویندوزی داشته باشد.
- بهبود سیستم تبدیل خودکار PNG مات به JPEG برای عملکرد دقیق‌تر در مواجهه با تصاویر دارای کانال آلفا.

### رفع شده (باگ‌ها)
- حل مشکل فریز و کرش شدن برنامه هنگام باز کردن پنجره انتخاب فایل/پوشه از طریق منوی کشویی تنظیمات.
- حل مشکل بهم‌ریختگی متن‌های دو زبانه (فارسی-انگلیسی) و علائم نگارشی مثل `|` و `-` در چیدمان RTL.
- حل مشکل باقی‌ماندن فایل‌های موقت و سنگین در پوشه `Temp` ویندوز پس از پایان فشرده‌سازی.
